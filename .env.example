# Bind to localhost unless LAN exposure is a deliberate choice.
BIND_ADDRESS=127.0.0.1
BACKEND_PORT=8000
FRONTEND_PORT=5173
# Optional comma-separated exact origins for alternate/LAN frontend URLs.
CORS_ORIGINS=
